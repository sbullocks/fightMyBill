import Box from '@mui/material/Box'
import Typography from '@mui/material/Typography'

export function PackPage() {
  return (
    <Box sx={{ p: 4, textAlign: 'center' }}>
      <Typography variant="h4">PackPage — Phase 4</Typography>
    </Box>
  )
}
